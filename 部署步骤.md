# 部署步骤（港澳行程手册）

目标：得到一个**公开网址**，任何人点开就能看，不用登录、不用装 App；你 push 代码，Cloudflare 自动上线。

**方案**：GitHub 仓库 + Cloudflare Workers（免费）。

---

## 各步骤一览

| # | 步骤 | 在哪里做 | 大概耗时 |
|---|---|---|---|
| 1 | 注册 GitHub 账号 | 手机或电脑都行 | 3 分钟 |
| 2 | 新建仓库并上传本项目文件 | **建议电脑** | 5 分钟 |
| 3 | 注册 Cloudflare 账号 | **建议电脑** | 3 分钟 |
| 4 | 在 Cloudflare 创建 API Token | **必须电脑** | 3 分钟 |
| 5 | 拿到 Cloudflare Account ID | **必须电脑** | 1 分钟 |
| 6 | 把两个值填进 GitHub Secrets | **必须电脑** | 3 分钟 |
| 7 | 触发首次部署并拿到网址 | **必须电脑** | 3 分钟 |
| 8 | 验证手机能打开 | 手机 | 1 分钟 |

> **必须在电脑上操作的**：第 4、5、6、7 步。原因写在对应步骤里。
> **手机上做会很别扭但理论上可行的**：第 1、2、3 步。
> **强烈建议全程用电脑**：一次做完省事。

---

## 第 1 步 · 注册 GitHub 账号

1. 打开 https://github.com/signup
2. 填邮箱、密码、用户名，完成邮箱验证。
3. 已有账号跳过这步。

**电脑上做**的理由：无。手机浏览器也能注册，只是填表慢一点。

---

## 第 2 步 · 新建仓库并上传文件

**建议在电脑上做。** 手机上传文件夹很麻烦，GitHub 网页版在手机上操作易出错。

1. 登录 GitHub，右上角 `+` → **New repository**
2. 填写：
   - **Repository name**：`hkmo-trip`
   - **Visibility**：选 **Public**（免费账号的 Workers 部署对私有仓库也支持，但 Public 配置最简单）
   - **不要**勾选 Add a README file
3. 点 **Create repository**
4. 在新页面点 **uploading an existing file**
5. 把项目里的这些文件/文件夹拖进去（**注意目录结构要保持**）：

```
wrangler.toml
public/index.html
src/worker.js
.github/workflows/deploy.yml
```

> 拖拽时，`.github` 这个以点开头的文件夹**拖拽可能不会被识别**。
> 如果拖不上去，就用下面的命令行方式（更稳）。

### 命令行方式（推荐，电脑上）

```bash
cd 项目所在目录

git init
git add .
git commit -m "港澳行程手册"
git branch -M main
git remote add origin https://github.com/你的用户名/hkmo-trip.git
git push -u origin main
```

首次 push 会让你登录 GitHub，按提示授权即可。

---

## 第 3 步 · 注册 Cloudflare 账号

**建议在电脑上做**（后两步都在电脑上，账号注册也是电脑上顺手）。

1. 打开 https://dash.cloudflare.com/sign-up
2. 填邮箱 + 密码，注册。
3. 去邮箱点验证链接。
4. 登录后，如果让你选套餐，**选 Free（免费）**。不需要绑信用卡。

> Cloudflare Workers 免费额度：每天 10 万次请求。你这是一张个人行程页面，用不掉。

---

## 第 4 步 · 创建 API Token ← **必须电脑**

**为什么必须电脑**：Cloudflare 后台的 Token 创建页面表单很长，手机上很容易点错权限。

1. 登录 Cloudflare Dashboard
2. 右上角头像 → **My Profile**
3. 左侧选 **API Tokens**
4. 点 **Create Token**
5. 找到 **Edit Cloudflare Workers** 这个模板，点右边的 **Use template**
6. 权限区确认包含：
   - `Account` → `Workers Scripts` → `Edit`
   - `Account` → `Workers KV Storage` → `Edit`（模板里自带，留着无妨）
   - `User` → `Memberships` → `Read`
   - `Zone` → `Workers Routes` → `Edit`（无用，可留）
7. **Account Resources** 选你自己的账号
8. 点 **Continue to summary** → **Create Token**
9. **立刻复制这串 Token**，页面关掉就再也看不到了。

> Token 长这样：`aBcD...` 一长串字母数字。
> **这串东西是密码级别的东西，不要发给任何人，不要贴到聊天里。**
> 万一泄露，回这个页面点 **Roll** 重新生成，旧的就失效。

---

## 第 5 步 · 拿到 Account ID ← **必须电脑**

1. Cloudflare Dashboard 首页
2. 右侧栏或 **Workers & Pages** 页面右侧，能看到 **Account ID**
3. 一串 32 位十六进制字符，复制它。

> 如果找不到：点右上角头像 → **My Profile** → 左侧 **Account** 或直接看 URL：
> `dash.cloudflare.com/<这一串就是 Account ID>/...`

---

## 第 6 步 · 把两个值填进 GitHub Secrets ← **必须电脑**

**为什么必须电脑**：GitHub Secrets 页面是个多步弹窗表单，手机上填长字符串容易出错。

1. 打开你的 GitHub 仓库页
2. 点 **Settings**（仓库的 Settings，不是账号的）
3. 左侧 **Secrets and variables** → **Actions**
4. 点 **New repository secret**，添加第一条：
   - **Name**：`CLOUDFLARE_API_TOKEN`
   - **Secret**：粘贴第 4 步复制的 Token
   - 点 **Add secret**
5. 再点 **New repository secret**，添加第二条：
   - **Name**：`CLOUDFLARE_ACCOUNT_ID`
   - **Secret**：粘贴第 5 步复制的 Account ID
   - 点 **Add secret**

> 名字必须**一字不差**，全大写，用下划线。写错了自动部署会失败。

---

## 第 7 步 · 触发部署，拿到网址 ← **必须电脑**

**为什么必须电脑**：要在 GitHub Actions 页面看构建日志，手机上没有这个页面。

1. 仓库页顶部 → **Actions** 标签
2. 左边选 **Deploy to Cloudflare Workers**
3. 如果有正在跑的构建，点进去看进度；如果没跑，点右侧 **Run workflow** → **Run workflow**
4. 等 1–2 分钟
5. 绿勾 = 成功；红叉 = 失败

### 成功后，网址在哪

构建日志里会打印类似：

```
Uploaded hkmo-trip
Deployed hkmo-trip triggers
  https://hkmo-trip.<你的子域名>.workers.dev
```

**那串 `https://hkmo-trip.xxx.workers.dev` 就是你的公开网址。**

也可以在 Cloudflare Dashboard → **Workers & Pages** → `hkmo-trip` 里看到同样的网址，还能在 **Settings → Domains & Routes** 里改名字。

### 失败怎么办

| 报错关键词 | 原因 | 解决 |
|---|---|---|
| `Authentication error` | Token 错了或已失效 | 重新做第 4 步，更新第 6 步的 Secret |
| `account ID` 相关 | Account ID 错了 | 重新做第 5、6 步 |
| `No such file` | 仓库里的目录结构不对 | 确认 `wrangler.toml`、`public/`、`src/` 在仓库根目录 |
| 其他 | — | 把日志贴给我，我看 |

---

## 第 8 步 · 验证（手机）

1. 手机浏览器打开那个 `workers.dev` 网址
2. 确认：倒计时在跳、地图能点开导航、颜色跟随白天黑夜
3. 想更像个 App：Safari 点分享 → **添加到主屏幕**；Chrome 点菜单 → **添加到主屏幕**

---

## 以后怎么更新

**你说改动，我改文件，你只要 push 一次，Cloudflare 自动上线。**

```bash
git add .
git commit -m "更新"
git push
```

推上去后，Actions 自动跑，1–2 分钟后网址上的内容就变了。你不需要再进 Cloudflare 后台做任何事。

---

## 关于「公开网址」的提醒

这个网址**任何人拿到都能打开**，Google 也可能收录。所以：

- 页面上**没有任何**确认号、订单号、证件号、房间号、团号
- 页面不含你的姓名和手机号
- 领队和紧急联系人的电话**也没写进页面**，请自己存在手机里

如果哪天不想公开了，去 Cloudflare Dashboard → Workers & Pages → `hkmo-trip` → **Settings** → 最下面 **Delete**，网址立刻失效。或者把 GitHub 仓库设为 Private 并删掉部署。

---

## 需要我代做的事

**仓库初始化、commit、Worker 配置**这些我可以在沙箱里准备好，你只需要：

1. 建好 GitHub 仓库
2. 用我给的命令把文件推上去
3. 在 Cloudflare 建 Token 和填 Secrets

其余（改页面、改内容、改配色）都交给我。
